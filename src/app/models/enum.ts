export enum URLS {
    BAR_CHART = 'bar-chart',
    PIE_CHART = 'pie-chart',
    LINE_CHART = 'line-chart',
    GROUPED_BAR_CHART = 'grouped-bar-chart',
    STACKED_BAR_CHART = 'stacked-bar-chart',
}
